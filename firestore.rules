rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Ứng dụng lớp học nội bộ – đồng bộ realtime
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
